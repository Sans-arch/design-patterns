public class Airport {

    // Interface como tipo, e não como um contrato a ser implementado
    // Este método está restringido a trabalhar somente com objetos que implementam
    // a interface FlyingTransport
    public void accept(FlyingTransport vehicle) {

    }
}
